[project]
name = "mintverse-docs"
version = "0.1.0"
description = "Render environment for the MINTverse documentation site."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "estimint>=1.5.4",
    "mintstate>=0.3.1",
    "matplotlib>=3.9",
    "jupyter>=1.1",
    "ipykernel>=6.29",
    "nbclient>=0.10",
]

[tool.uv]
package = false
